# This directory stores conversation logs as JSON files
# Files are generated at runtime when conversations end through the "end conversation" button or when the session is closed
# Contents are gitignored for privacy
